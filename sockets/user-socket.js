// const { userToSocket, socketToUser } = require("../index");
const User = require("../models/User");
const { formatUserData } = require("../utils/helpers/addUserName");
module.exports = async (socket, userToSocket, socketToUser) => {
  // Send Friend Request
};
